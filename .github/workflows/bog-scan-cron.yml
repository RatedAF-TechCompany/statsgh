name: BoG Dashboard Update Scanner

on:
  schedule:
    # Run every 12 hours at minute 30
    - cron: '30 */12 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  trigger-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger BoG Dashboard Scan
        run: |
          curl -X POST \
            "https://ofhejtwaigiqyejbvncz.supabase.co/functions/v1/bog-dashboard-scan" \
            -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9maGVqdHdhaWdpcXllamJ2bmN6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjMyOTE1NjUsImV4cCI6MjA3ODg2NzU2NX0.l01PfzD7KDaGQJKRoLFxoBuA46z8OsAM7F0Xc4DTLEo" \
            -H "Content-Type: application/json" \
            -d '{"triggerType": "scheduled"}' \
            --fail-with-body \
            --max-time 300
